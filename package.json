{
  "name": "@football-is-life/source",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "type": "module",
  "scripts": {
    "build": "bun run --filter=* build",
    "dev": "bun run --filter=* dev",
    "start": "bun run --filter=* start",
    "test": "bun run --filter=* test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "5.5.4"
  }
}